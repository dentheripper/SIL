<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF">

    <ImageButton
        android:id="@+id/back_cn"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:background="@null"
        android:contentDescription="@string/todo"
        android:src="@drawable/ic_baseline_arrow_back_ios_24" />

    <EditText
        android:id="@+id/cn_name"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@id/back_cn"
        android:autofillHints="name"
        android:backgroundTint="#FFF"
        android:ems="10"
        android:inputType="text"
        android:paddingStart="10dp"
        android:textColor="#000"
        android:textColorHint="#999"
        android:textSize="17sp"
        tools:ignore="LabelFor,RtlSymmetry" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/constraintLayout3h"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/cn_name"
        android:layout_marginTop="5dp"
        android:background="#335" />

    <EditText
        android:id="@+id/cn_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/constraintLayout3h"
        android:autofillHints="name"
        android:background="@null"
        android:ems="10"
        android:gravity="top|left"
        android:inputType="textMultiLine"
        android:padding="10dp"
        android:textColor="#000"
        tools:ignore="LabelFor,RtlHardcoded" />

</RelativeLayout>